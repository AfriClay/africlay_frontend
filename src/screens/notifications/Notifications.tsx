import React from 'react';
import { EmptyState } from '../../components/ui/EmptyState';

export const Notifications: React.FC = () => <EmptyState title="Notifications unavailable" description="Account notifications are not supported by the backend yet." />;
